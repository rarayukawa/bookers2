

             <% if book.errors.any? %>
            <h3><%= book.errors.count %>errors prohibited this book from being saved:<br>
            <ul>
            <% book.errors.full_messages.each do |message| %>
            <li><%= message %></li>
            <% end %>
            </ul>
            <% end %>
          <h1>User info</h1>
          <table class="table">
            <div class="profile-container">
               <%= attachment_image_tag user, :profile_image, :fill, 100, 100, format: 'jpeg', fallback: "no_image.jpg" %>
                 <h3 class="profile-name">Name</h3><%= user.name %>
                 <h3 class="profile_introduction">Introduction</h3><%= user.introduction %>

                  <%= link_to edit_user_path(user), class: "btn btn-light btn-lg btn-block" do %>
                  <i class="fas fa-user-cog"></i>
                    <% end %>
            </div>
            </table>
          <h1>New book</h2>
            <%= form_with model:book, local:true do |f| %>
            <p><%= f.label :title, "Title" %><br>
            <%= f.text_field :title %></p>
            <p><%= f.label :body, "Opinion" %><br>
            <%= f.text_area :body %></p>

            <%= f.submit 'Create Book', class: "btn btn-success" %>
            <% end %>
        <br>
